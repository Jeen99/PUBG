<Window x:Class="BattleRoyalClient.BattleView3d"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BattleRoyalClient"
        mc:Ignorable="d"
        Title="BattleView3d" Height="500" Width="500">
    <Grid>
        <Viewport3D x:Name="viewport" ClipToBounds="True">
            <Viewport3D.Camera>
                <PerspectiveCamera x:Name="camera" FarPlaneDistance="1000" NearPlaneDistance="0.1" Position="0, 0, 200" LookDirection="0, 0, -1" UpDirection="0, 1, 0"/>
            </Viewport3D.Camera>
            <ModelVisual3D>
                <ModelVisual3D.Content>
                    <Model3DGroup x:Name="models">
                        <!--Источники света-->
                        <AmbientLight Color="#333"/>
                        <DirectionalLight Color="#FFF" Direction="-1, -1, -1"/>
                        <DirectionalLight Color="#FFF" Direction="1, -1, -1"/>
                    </Model3DGroup>
                </ModelVisual3D.Content>
            </ModelVisual3D>
        </Viewport3D>
        <ProgressBar  x:Name="HP" Margin="40,0,0,15" VerticalAlignment="Bottom" MinWidth="100" MinHeight="10" HorizontalAlignment="Left"/>
    </Grid>
</Window>
